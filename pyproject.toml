[project]
name = "conceptual-design-engine"
version = "0.1.0"
description = "Manual zoning editor with 3D massing for the conceptual design phase of a house."
requires-python = ">=3.10"
dynamic = ["dependencies"]

[tool.setuptools.dynamic]
dependencies = { file = ["requirements.txt"] }

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = [
  "E501",  # long lines: prompts and messages are prose; the formatter handles code
  "B008",  # pydantic Field() in defaults is the idiom
]

[tool.ruff.lint.isort]
known-first-party = ["api", "tests"]
