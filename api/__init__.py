"""The thin HTTP layer under the zoning editor.

The editor -- the TypeScript canvas, schedule and 3D view in frontend/ --
owns the arrangement and every rule about it. This package only serves
the built app and keeps saved layouts; nothing architectural lives here.
"""
