__pycache__/
*.pyc
.venv/
venv/
.env
.streamlit/secrets.toml
.pytest_cache/
*.egg-info/
data/

# frontend
frontend/node_modules/
frontend/dist/
# TypeScript's incremental-build cache. Rewritten by every build, so
# tracking it made each machine's build collide with the next pull.
*.tsbuildinfo
